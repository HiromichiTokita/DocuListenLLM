# GUI
customtkinter>=5.2.0

# LLM (Ollama ローカルAPI)
ollama>=0.4.0

# 音声合成・再生・処理
numpy>=1.24
sounddevice>=0.4.6
soundfile>=0.12
pedalboard>=0.9

# HTTP / システム
requests>=2.31
psutil>=5.9

# ファイル取込（.pdf / .docx / .epub）
pypdf>=4.0
python-docx>=1.1
ebooklib>=0.18
beautifulsoup4>=4.12

# ドラッグ&ドロップ（任意・未導入でもD&Dが無効になるだけ）
windnd>=1.0
